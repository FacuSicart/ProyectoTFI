@model ProyectoTFI.Models.CursoViewModel

@{
    ViewBag.Title = "Deshabilitar Curso";
    Layout = "~/Views/Shared/_LayoutInterno.cshtml";
}

@{
    string estadoMostrado = (bool)Model.Activo ? "Activo" : "Inactivo";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Deshabilitar Curso</h4>
        <hr />
        <h5>¿Está seguro que quiere deshabilitar al Curso seleccionado?</h5>
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ID)

    <div class="" style="background-color: antiquewhite; border-radius:10px;padding:10px;">
        @if (ViewData["ErrorMensaje"] != null)
        {
            <div class="alert alert-danger" role="alert">
                @ViewData["ErrorMensaje"]
            </div>
        }
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="row">
            <div class="col-md-6">
                <div class="form-group">
                    @Html.LabelFor(model => model.ID, htmlAttributes: new { @class = "control-label" })
                    <div class="col-md-10">
                        @Html.EditorFor(model => model.ID, new { htmlAttributes = new { @class = "form-control", @disabled = "disabled" } })
                        @Html.ValidationMessageFor(model => model.ID, "", new { @class = "text-danger" })
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="form-group">
                    @Html.LabelFor(model => model.Nombre, htmlAttributes: new { @class = "control-label " })
                    <div class="col-md-10">
                        @Html.EditorFor(model => model.Nombre, new { htmlAttributes = new { @class = "form-control", @disabled = "disabled" } })
                        @Html.ValidationMessageFor(model => model.Nombre, "", new { @class = "text-danger" })
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-6">
                <div class="form-group">
                    @Html.LabelFor(model => model.Descripcion, htmlAttributes: new { @class = "control-label" })
                    <div class="col-md-10">
                        @Html.TextAreaFor(model => model.Descripcion, new { @class = "form-control", @rows = 4, @disabled = "disabled" })
                        @Html.ValidationMessageFor(model => model.Descripcion, "", new { @class = "text-danger" })
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-6">
                <div class="form-group">
                    <label for="listaDocentes">Docentes:</label>
                    <select id="listaDocentes" name="Docentes" class="form-control" multiple="multiple" size="5">
                        @foreach (var docente in Model.Docentes)
                        {
                            <option value="@docente">@docente.Usuario.Apellido, @docente.Usuario.Nombre</option>
                        }
                    </select>
                </div>
            </div>
        </div>
        <br />
        <div class="row gy-5">
            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Deshabilitar" id="btnSubmit" class="btn btn-success" />
                    @Html.ActionLink("Volver", "VerCursos", "Curso", new { area = "" }, new { @class = "btn btn-success" })
                </div>

            </div>
        </div>
    </div>
    </div>
}